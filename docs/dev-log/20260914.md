# 2026-09-14 开发日志

## 今日完成

- 分析 `design/fork_video.mp4`，还原核心数据链路：摄像头 → MediaPipe 双手关键点 → 两手拇指/食指四点 → Corner Pin 透视变换 → 滤镜合成 → 实时预览。
- 确定使用独立 Python 应用复刻最终效果，不依赖 TouchDesigner，也不交付 `.toe` 节点工程。
- 确定首版范围：Python 3.11、实时窗口、核心彩色滤镜效果；暂不包含录像、虚拟摄像头、多滤镜和手势菜单。
- 搭建第一阶段骨架：数据类型与 `HandTracker` 接口、四角排序、`WAITING/TRACKING` 状态机、Mock 追踪器、启动入口和边界测试。
- Mock 启动与核心测试通过。
- 初始化 Git 仓库，创建 `main` 分支，并将首个 README 提交推送到 GitHub。

## 遇到的问题

- 系统没有将 FFmpeg 加入 PATH，但复用了本机 Conda 环境中的 FFmpeg 完成视频抽帧，没有安装新依赖。
- 基础 Python 为 3.13，现有 Conda 环境为 3.8；MediaPipe 官方支持 Python 3.9–3.12，因此决定新建 Python 3.11 环境。
- `.git` 由隔离账户创建，目录所有者与当前 Windows 用户不一致，导致 Git 和文件补丁工具无法继续访问工作区。
- MediaPipe、OpenCV、透视合成和环境脚本的实现补丁因上述权限问题未写入，不能记为已完成。

## 明日计划

1. 修复 `.git` 所有者和 ACL，确认补丁工具恢复。
2. 创建 Python 3.11 Conda 环境，安装并验证 MediaPipe 1.0.1。
3. 接入摄像头和 MediaPipe 双手追踪，输出拇指尖与食指尖坐标。
4. 实现程序化彩色滤镜、OpenCV 透视变换和透明遮罩合成。
5. 运行核心测试、渲染测试和摄像头实机验收。

## 当前阻塞

以管理员 PowerShell 修复 `.git` 权限：

```powershell
icacls "D:\Documents\hand_tracking\.git" /setowner "LAPTOP-2248U6C2\Avenir" /T /C
icacls "D:\Documents\hand_tracking\.git" /grant:r "LAPTOP-2248U6C2\Avenir:(OI)(CI)F" /T /C
```
